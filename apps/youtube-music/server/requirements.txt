# pip install -r requirements.txt
# 別途、コマンドとして ffmpeg が必要 (macOS: brew install ffmpeg / Debian: apt install ffmpeg)
ytmusicapi>=1.7
qrcode>=7
Pillow>=10
# yt-dlp は PATH 上のコマンドとして呼ぶが、venv にも入れておく。
# Linux の apt/dnf 版は古く、YouTube 側の変更で壊れるため
# (Raspberry Pi では systemd の PATH を venv 優先にして使う。
#  docs/raspberry-pi-server.md 参照)
#
# [default] を必ず付ける。素の yt-dlp には yt-dlp-ejs が入らず、
# YouTube の JS チャレンジを解けないため **取得できる形式が消える**。
# 症状は「Requested format is not available」で、
# ログインしていても曲が取れず無音になる (実際に踏んだ)
yt-dlp[default]>=2025.6
